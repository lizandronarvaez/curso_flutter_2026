import 'package:flutter/material.dart';

class VideBackground extends StatelessWidget {
  const VideBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return Positioned.fill(child: const Placeholder());
  }
}
